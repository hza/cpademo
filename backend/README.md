# Backend

This folder contains the backend Python project extracted from the repository root.

See the top-level README for full usage; run commands from the repository root or from this folder.

To install dependencies:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Run the CLI from this folder with `python main.py`.
